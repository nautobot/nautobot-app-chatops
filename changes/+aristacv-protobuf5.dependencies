Updated the `aristacv` (Arista CloudVision) extra to require `cloudvision>=1.25` and `protobuf>=5,<6`, and migrated the CloudVision device-tag lookup from the `arista.tag.v1` to the `arista.tag.v2` API, restoring compatibility with Nautobot 3.x, which requires protobuf 5.
